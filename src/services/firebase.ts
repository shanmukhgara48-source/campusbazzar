/**
 * Firebase has been removed. This file is a stub so any missed import
 * fails gracefully with a clear error instead of a module crash.
 *
 * If you see "auth is null" or "db is null" errors, find that import
 * and replace it with the equivalent from src/services/api.ts.
 */

export const db   = null as any;
export const auth = null as any;
